== 口径②：真实上游首 token 分布（2026-08-04 03:22:54Z）==
测量点=直连 build_gateway().complete() 首个 TextDelta（D2：不过 HTTP/SSE——两组口径正交）
N=100（目标 100，异常 0）；standard 档串行；prompt 唯一化；缓存关；注入关；max_tokens=64
不做高并发：费用与厂商限流约束（04 M5 原文理由）

P50 = 1306 ms    P90 = 2118 ms    P99 = 4116 ms
min = 1030 ms   max = 4116 ms

直方图（ms 桶 | 样本数）：
       0-400 | (0)
     400-600 | (0)
     600-800 | (0)
    800-1000 | (0)
   1000-1500 | ################################################################## (66)
   1500-2500 | ############################# (29)
       >2500 | ##### (5)

真实花费：ledger 记 ¥0.0000——**这是计量盲区不是零调用**（首块即 aclose，流未到
UsageChunk 故计量单点无账可记；供应商侧照常计费）。诚实上界估算：100 次 ×
（输入 ≈30 tok + 输出 ≤64 tok）≈ ≤9.4k token ≈ **≤¥0.03**（护栏 ¥1.0 未触及）。
与口径① 对照：模拟参数 800ms 相对实测 P50 1306ms 偏乐观——口径① 报告读数时须带本句。
