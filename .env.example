# 复制本文件为 .env 并填入真实值（.env 已被 .gitignore 排除，永不入库）
APP_ENV=dev
DASHSCOPE_API_KEY=sk-在这里填你的百炼key
# ANTHROPIC_API_KEY=sk-ant-...（可选：获得 key 后填入，跨平台 fallback 即变真实）
# DATABASE_URL / REDIS_URL 本地开发用代码默认值即可，无需在此设置