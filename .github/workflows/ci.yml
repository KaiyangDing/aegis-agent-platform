name: CI

on:
  push:
    branches: [ main ]
  pull_request:

# 最小权限：本 workflow 只需要读代码。GITHUB_TOKEN 默认权限过宽（审计加固 B）
permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: aegis
          POSTGRES_PASSWORD: aegis
          POSTGRES_DB: aegis
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U aegis -d aegis"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    steps:
      # action 钉到完整 commit SHA：tag 可被上游重指恶意提交，SHA 不可变（审计加固 B）
      - name: 检出代码
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: 安装 uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5
        with:
          enable-cache: true          # 缓存依赖下载，第二次起 CI 明显变快

      - name: 安装依赖（严格按 uv.lock，锁不同步直接失败）
        run: uv sync --frozen

      - name: 代码检查（ruff）
        run: uv run ruff check .

      - name: 类型检查（mypy）
        run: uv run mypy aegis

      - name: 架构分层检查（import-linter）
        run: uv run lint-imports

      - name: 数据库迁移（alembic 在 CI 真实执行）
        run: uv run alembic upgrade head

      - name: 测试（pytest）
        run: uv run pytest