# Aegis 项目会话入口（M4.7 起主入口——启动序列与八条硬规则在此）

> 本文件是防错闸门：**任何工作开始前，先按下面的启动序列执行**。
> 文档体系按"接续模型零脑补"标准加固——你（当前模型）的职责是**严格按文档执行**，
> 而不是自由发挥。设计文档自 M4.7 起在仓库 `docs/`（此后文档变更走提交）。

## 项目一句话

Aegis：企业级多租户客服 Agent 平台（用户的简历项目，目标岗位 AI Agent/应用开发）。
三层架构 L1 网关 → L2 自研运行时 → L3 客服业务。"LLM 是 CPU，Aegis 是操作系统"。

## 位置

- 代码仓库：本 repo（Python 3.13 + uv）
- 文档目录：`docs/`（M4.7 迁入仓库；`docs/00-master-plan.md` 是执行主文档）
- 记忆档案（**仓外**）：`C:\Users\28427\.claude\projects\D--emptyfolder\memory\aegis-agent-platform.md`
- 仓外上层目录另有指针版 `CLAUDE.md`（在上层目录开会话时自动加载，指回本文件）

## 启动序列（新会话第一件事，按序执行，不许跳步；与 00 §0 一致，冲突以 00 为准）

1. 读记忆档案 `aegis-agent-platform.md`（仓外路径见上）；
2. 读 `docs/07-handoff-guide.md` 全文（接续开发驾驶手册——协作规约、红线、交付模板都在里面）；
3. 读 `docs/00-master-plan.md` §1–§3 定位当前里程碑与当前步骤，
   再读**当前里程碑章节全文**与 **§10 横切清单**；
4. 按 00 **§12 文档地图**读该里程碑的必读设计文档（`docs/08-code-map.md` 是接口事实源快照）；
5. 读当前步骤对应的 `docs/plans/` 计划文件，**先执行其"§0 开工核对清单"再动手**；
6. 仓库对账：`git tag`、`git log --oneline -5`、`uv run pytest -q` 收集数与 00
   最新对账表末行登记基线核对（对不上先停下报用户）；
7. 给出本步全景图（或按 00 §0 给里程碑全景图），经用户确认后再进入交付。

## 八条硬规则（违反任何一条 = 本次交付无效）

1. **生产代码用户亲手敲**——你给完整代码+逐段解释，绝不直接写入 `aegis/` 包下的产品代码
   （单步例外须用户明示并登记：M2.10、M4.6–M4.8 全量委托已在 00 §2.1 登记）；
   **测试代码你直接写入**；设计文档/计划/记忆由你直接维护。
2. **验证与 git 指令用户执行**——你给完整指令块并标注预期结果；仅用户说"验收"时你才代跑核对
   （M4.6 起的全量委托例外同上）。
3. **测试与 CI 全程零真实 API 调用**（各里程碑一次性例外见 00 各章首）。
4. 口径冲突时信任序：**实际代码 > 00 §2.2 口径表 > docs/plans/ 计划 > 设计文档叙述**；
   发现冲突当天登记修订，不留口头约定。
5. **每次交付只给一步**；不顺手重构、不顺手加范围（动手前查 00 §10.3 v1/v2 边界）。
6. **已推送历史不改**；小步提交，message 写"为什么"；一步没提交就不算完成。
7. **验收做清单对账**：逐项点名新增测试/文件 + pytest 收集数核对，绝不对未核查部分说"全部正确"。
8. **引用任何接口前先 Read 真实源码核实签名**——不许凭记忆或凭文档转述写代码。

## 环境速记（详见 docs/06 与 docs/07）

Windows 11 本地开发；shell 是 PowerShell；Celery 本地用 `--pool=solo`（容器内 Linux 用默认 prefork）；
uv 管依赖（**绝不用 pip / PyCharm 装包**——会改坏 pyproject，见记忆档案教训）；
脚本写文件一律 `Path(__file__)` 锚定项目根，不依赖 cwd。
