# Aegis 仓库会话入口

本仓库的执行主文档与全部设计文档在仓库外：`D:\emptyfolder\docs\`（M4.7 才迁入仓库）。
项目级会话入口在 `D:\emptyfolder\CLAUDE.md`——**先读它，按其"启动序列"与"八条硬规则"执行**。

最关键三条（完整版见项目级 CLAUDE.md）：

1. 生产代码（`aegis/` 包）**用户亲手敲**，你只给完整代码+讲解；测试代码你直接写入。
2. 测试与 CI **零真实 API 调用**。
3. 引用任何接口前先 Read 真实源码核实签名；验证/git 指令给用户执行并标预期结果。
